# Research report — comprehensive-documentation-engineering

Synthesis project record. Retrieval date for all sources: **2026-08-20** (local clones and live fetches performed the same day). All revisions are the default branch HEAD at retrieval time.

## 1. Source inventory

| # | Source | Author / maintainer | URL | Branch | Commit SHA | License | Primary purpose |
|---|---|---|---|---|---|---|---|
| 1 | Diátaxis article: "Diataxis Meets AI" | Romain Lespinasse | https://www.romainlespinasse.dev/posts/diataxis-documentation-skill/ | — (web) | — | site content, all-rights-reserved assumed; ideas only used | Motivation and 4-step workflow (discover → classify → propose → execute) for the agent-skills diataxis skill |
| 2 | agent-skills | Romain Lespinasse | https://github.com/rlespinasse/agent-skills | main | `a6c5b6c3ec48f13aca46aa10c206aeb5980a6fd7` | MIT | Multi-skill repo; diataxis skill + agentskills.io packaging, promptfoo eval infrastructure |
| 3 | diataxis-agent-skill | Keith Patton | https://github.com/keithpatton/diataxis-agent-skill | main | `5b095a5e7aebe77ce4af855809123e12d92b9efd` | CC BY-SA 4.0 | Governance-flavored Diátaxis skill: classify / audit / generate / restructure with confidence+evidence output and refusal-as-success |
| 4 | diataxis-docs-skill | Peter Knego | https://github.com/peterknego/diataxis-docs-skill | main | `de11e3bb2dd56d04849f70d8b1964b5593d369ec` | MIT AND CC-BY-SA-4.0 (split) | Gated, phased doc-set generator: survey → plan file → approval → quadrant-ordered writing → assembly; execution-verified tutorials; C4 diagrams |
| 5 | Claude Marketplaces listing | claudemarketplaces.com (third-party) | https://claudemarketplaces.com/skills/sammcj/agentic-coding/writing-documentation-with-diataxis | — (web) | — | interpretive summary only | Marketplace metadata + install command for source 6. **Third-party interpretation, not first-party source** |
| 6 | agentic-coding (`Skills_disabled/diataxis-documentation`) | Sam McLeod (sammcj) | https://github.com/sammcj/agentic-coding | main | `25cb214728a01bd3ad3b469d2e104f25156b52d4` | Apache-2.0 | Anti-template Diátaxis writing skill; deep principles + quality framework + complex-scenario guidance |
| 7 | developer-docs-framework | Anivar Aravind | https://github.com/anivar/developer-docs-framework | main | `c0e9445fcdf832681db78eaca58cdcefccd1e654` | MIT (methodology sources have own licenses) | Enterprise docs framework: 27 rules, 14 content types, 6 pluggable style guides, maturity model, adoption funnel, DX/partner/governance rules |
| 8 | Diátaxis (diataxis.fr), via source repo evildmp/diataxis-documentation-framework | Daniele Procida | https://diataxis.fr/ · https://github.com/evildmp/diataxis-documentation-framework | main | `957c09ca40b4a1edc23874f713e01937d50d54d5` | CC BY-SA 4.0 | The authoritative Diátaxis doctrine — controls all claims about Diátaxis theory, terminology, and recommended practice |
| 9 | docs-and-release-rig | Courtney Robertson | https://github.com/courtneyr-dev/docs-and-release-rig | main | `d9ca62de483e91708821f963cc919f7dfd47fa88` | CC0 1.0 | Docs-as-testable-claims: coverage map, change-coverage receipt, UI/label verification, source hierarchy, beta/RC runbook, evidence ladder, audit handoff standard |

Authorship was verified from each source itself (LICENSE files, CITATION.cff, colophon, README bylines), not inferred from the requester.

## 2. File inspection record

Legend: **full** = read in entirety; **skim** = headings/structure verified, content confirmed duplicative of a fully-read file; **n/a** = binary or irrelevant to capability extraction.

### Source 2 — agent-skills (53 files)
- **full**: `skills/diataxis/SKILL.md`, `skills/diataxis/references/{compass,tutorials,how-to-guides,reference,explanation,quality}.md`, `skills/diataxis/evals/evals.json`, `.claude-plugin/marketplace.json`, `README.md`, `CLAUDE.md`, `docs/reference-skill-spec.md`, `docs/tutorial-create-a-skill.md`, `just/evals.just`, `scripts/check-evals.js` (head), `scripts/eval-provider.js` (head), `skills/verify-readme-features/SKILL.md` (article-linked, feeds EVID capabilities).
- **skim**: `CONTRIBUTING.md`, `SECURITY.md`, `CHANGELOG.md`, remaining `just/*.just`, `.github/workflows/*` (CI/autofix/release/evals wiring — capability captured as PKG-level facts), `scripts/{create-evals-boilerplate,generate-eval-config,update-marketplace}.js`.
- **full (added 2026-08-20 at user direction, second pass)**: the six sibling skills' SKILL.md files — `conventional-commit`, `drawio-export-tools`, `french-language`, `local-branches-status`, `pin-github-actions`, `verify-pr-logs` — incorporated as the AUX-* ledger capabilities into `references/repo-workflow-practices.md` (their evals.json files remain skimmed: same schema as the diataxis evals already captured).
- **n/a**: lint configs, dependabot.

### Source 3 — diataxis-agent-skill (8 files) — **all read full**
README.md, CITATION.cff, LICENSE, .gitattributes, `diataxis/SKILL.md`, `diataxis/references/{compass,quadrants,anti-patterns}.md`.

### Source 4 — diataxis-docs-skill (41 files)
- **full**: root README, `diataxis-plan.md` (the repo's own dogfooded plan file), `skills/diataxis-docs/SKILL.md`, `skills/diataxis-docs/README.md`, all six `references/*.md`, `docs/index.md`, `docs/how-to/{install-and-update,run-on-a-repo}.md`, `docs/reference/plan-file.md`, `docs/explanation/design.md`, both design specs under `docs/superpowers/specs/`, `.claude-plugin/{plugin,marketplace}.json`.
- **skim**: `docs/superpowers/plans/*` (implementation task lists; confirmed by heading sweep to restate the specs verbatim — no unique capability), `site/txt/*` (verbatim mirror of diataxis.fr, cross-checked against source 8's canonical repo; the mirror's `complex-hierarchies.md` confirmed the page recovered from source 8's git history).

### Source 6 — agentic-coding (diataxis scope) — **all read full**
`Skills_disabled/diataxis-documentation/{SKILL.md,principles.md,reference.md}`; repo README + `Skills/` layout skimmed for packaging context. The marketplace name `writing-documentation-with-diataxis` matches the SKILL.md frontmatter `name:`.

### Source 7 — developer-docs-framework (43 files) — **all read full except as noted**
README.md, SKILL.md, all 27 `rules/*.md`, `references/{content-types,templates,style-guide,anti-patterns,enterprise-patterns}.md`, `references/styles/{diataxis,google,microsoft,stripe,canonical,minimal}.md`. **skim**: `AGENTS.md` (verified by header/heading comparison to be the mechanical compilation of SKILL.md + rules + references already read), `.github/FUNDING.yml` (n/a).

### Source 8 — diataxis.fr source (241 files) — **all content pages read full**
`source/{index,start-here,compass,application,theory,foundations,map,quality,how-to-use-diataxis,tutorials,how-to-guides,reference,explanation,tutorials-how-to,reference-explanation,translation,colophon,news}.rst` — the complete current site. Plus **`complex-hierarchies.rst` recovered from git history** (removed from the live site in commit `abfae1f` "Tidied up some files; removed complex hierarchies page"); its content survives in the synthesis as historical-but-source-verified guidance, flagged as such. `CITATION.cff`, `LICENSE.rst` read. **n/a**: images, Sphinx `conf.py`, theme/templates, `translation/` PO files (mechanics of the translation program captured from `translation.rst`), `tests/`.

### Source 9 — docs-and-release-rig (17 files) — **all read full**
README.md, SETUP.md, skills-and-tooling.md, all of `1-documentation-practice/01–05`, `2-release-testing/{06,07,11}`, `3-security-methods/{08,09,10}`, LICENSE. **skim**: `index.html` (self-described rendered copy of README), `.gitignore` (n/a).

### Sources 1 & 5 — web pages
Fetched live 2026-08-20 and content-extracted in full. Source 5 was confirmed to add only interpretive summary + install metadata on top of source 6; nothing from its interpretation is treated as first-party doctrine.

## 3. Unique contributions per source (what must survive)

| Source | Contributions that no other source provides |
|---|---|
| 1 (article) | The 4-step discover→classify→propose→execute frame; explicit rationale that skills embed methodology into tooling so structure holds regardless of contributor (human or AI); "no automated process should reorganize your docs without you signing off" |
| 2 (rlespinasse) | Git-history respect for prior deliberate reclassification; boundary-case rules (troubleshooting/FAQ/migration/README classified by structure and intended use, not subject); classification-results table format; docs-tooling adaptation rules (mkdocs/docusaurus/antora/hugo); per-quadrant page templates; evals.json schema + promptfoo/claude-CLI eval harness + structural eval validator; agentskills.io packaging conventions (≤500-line SKILL.md, description pattern "what + Use when + scope boundary"); verify-readme-features: claim-status taxonomy (Confirmed/Partial/Not found/Overstated), sub-claim decomposition, evidence citation (file:line), "docs are the claims, not the proof" |
| 3 (keithpatton) | Explicit operating modes (classify/audit/generate/restructure) with mode-inference + clarification rule; generate-mode policy (stated quadrant OR classify-confirm); classification output contract (quadrant + confidence high/med/low + evidence); confidence calibration table for violations; refusal-as-success; violation-detection signal lists per anti-pattern with failure modes and compliant counterexamples; blur-zone table; non-goals section; CC BY-SA compliance model with CITATION.cff |
| 4 (peterknego) | Clean-tree git gate; persistent plan file (transient/persistent section lifecycle, trim-and-commit); reference scope as separately-approvable item; not-created entries requiring reason + remedy; keep/move/split annotations with mandatory destinations; parking lot; quadrant writing order (reference→how-to→explanation→tutorial) with dependency/cost rationale; batched single question round; move-is-a-rewrite rule; section-granularity compass self-checks; autodoc rule (improve the pipeline, never parallel pages; section-level too); measured-numbers-carry-provenance; tutorial execution-verification protocol (sandbox, no side effects, normalize output, honest unverified markers); README wiring; standing-decisions re-verification; C4 diagram rules (Context+Container only, evidence-traced elements, thin-repo rule, Mermaid stable subset, no style overrides, prose carries the why); explicit-invocation-only guard for heavy workflows; license-split practice |
| 6 (sammcj) | "Approach, not template" as an operating instruction; why-exactly-four grounding; per-type review question set; boundary-check checklist while writing; "flow is paramount" articulation for how-to guides (what must the reader hold open?); functional-vs-deep quality as constraints-vs-liberation; multi-user-type / multi-environment structuring options; landing-page and list-length practical guidance restated for agents; "what Diataxis cannot do" limits; minimalist-documentation guidance (not all four types per feature); living-documentation rationale; per-type practical tips for writing and reviewing; review sign lists (boundary violations, missing flow, wrong audience); British-English house-style example of a skill-level style constraint |
| 7 (anivar) | 27 atomic rules with incorrect/correct example pairs; 14 enterprise content types (incl. quickstart, integration guide, migration guide, troubleshooting, SDK/config reference, changelog, architecture guide, glossary, runbook); pluggable style-override system (Diátaxis default + Google/Microsoft/Stripe/Canonical/Minimal); adoption funnel (Discover→Evaluate→Start→Build→Operate→Upgrade) and bottleneck prioritization; audience matrix; time-to-hello-world metric; interactivity ladder; maturity model (Seeds→Foundation→Integration→Excellence); Google OpenDocs project archetypes; audit process (inventory→classify→gap→prioritize→plan); docs-as-code workflow + CI checks; versioning strategy + lifecycle (Preview→Current→Supported→Deprecated→Archived); freshness cadence table; docs-are-done governance incl. per-change-type docs requirements; partner both-sides + production-readiness checklist; measurement metrics (quant + qualitative, vanity-metric warning); localization practices; consolidated anti-pattern checklist with memorable names; ready-to-use templates for every content type + ADR + glossary entry; training-data distrust warning |
| 8 (diataxis.fr) | The authoritative doctrine: four needs/forms, map, compass, foundations (two dimensions of craft; completeness argument), functional vs deep quality theory, all per-type principles and language exemplars, tutorials-vs-how-to and reference-vs-explanation deep treatments (incl. basic≠tutorial/advanced≠how-to; medical analogies), the workflow ("use as a guide not a plan", "don't create empty structures — it's horrible", one small step at a time, organic growth, complete-not-finished), complex-hierarchies guidance (landing pages read as overviews; ~7-item list rule; two-dimensional structures; user-first resolution), translation program mechanics (translate meaning not words; native key terms; start with Start-here; placeholders; context awareness), attribution/citation requirements |
| 9 (docs-and-release-rig) | Docs-as-testable-claims thesis; source hierarchy (runtime test > dev note > diff > community roundup > memory; reading code below a dev note); 48-hour knowledge-decay rule + daily sweep + dated update log ("readers need the delta"); milestone triggers (beta/RC1/RC2/GA); coverage map (claims vs verification status, originality guardrail, gap table grouped by audience, quality-of-life clustering, cross-deliverable gap discovery); "a gap written down is a task"; change-coverage receipt (11 reconciliations + receipt table + hard gate; "announced-but-didn't-ship" class); UI verification & label-diff (accessibility-tree probe first; screenshot pass = free doc audit; label-diff as required deliverable; content realism; editor server-side state; automation gotchas; contrast rule); beta/RC runbook (environments grouped by update-timing; four-phase per-drop loop; small human checklist; definition of done; go/no-go); environment-fidelity ladder + wrong-build risks + upgrade-path gotcha; public testing tooling patterns (blueprint + checklist = two-click community testing; README documents its own limitations); external-PR audit case (held CI ≠ green; probe both branches; make guards fail once; compat header = documentation claim with stated evidence ceiling; credit the contributor); evidence & validation standard (9-rung gate ladder; 20-step loop; prove-by-firing; no-third-door; controls from external corpus; consensus-is-not-truth; fired vs source-attested labels; null-result rule; bounded-environment escape hatch; safe-proof minimality table; reporting field order; hypothesis/blocked-route/disproved/finding/regression templates); audit handoff standard (self-contained single file; public-links-only verified while logged out; confidentiality banner; required section order; tested-and-cleared section; honest gaps; verbatim prompts; used/staged/available/authored labels; bare-acknowledgment scrubbing + grep-proof; re-verify findings live); docs-site IA (5 sections; accessibility & privacy as own pages); repo docs split (CONTRIBUTING/TESTING/DEPLOYMENT/BRANCHING + SECURITY private channel); repo-specific lint for recurring review findings; skill-description advice ("when to use" beats "what it is") |

## 4. Overlaps

- Compass/map/four types: sources 2, 3, 4, 6, 7 all restate source 8. Canonical rule taken from 8; implementation detail merged from the others (see ledger `DIA-*`).
- Anti-scaffold / "approach not template": 4, 6, 7, 8 (canonical: 8).
- Mixed-content detection & split: 1, 2, 3, 4, 6, 7 (merged into `AUD-CLASS-*` and `WRITE-MIX-*`).
- Approval before restructuring: 1, 2, 3, 4 (+ implicit in 9's gates). Merged into `SAFE-*`.
- Code examples must work / tested in CI: 7, 9 (+ 4's execution verification). Merged into `EVID-EXEC-*`.
- Landing pages / list rule / hierarchy: 4, 6, 8 (canonical: 8's complex-hierarchies).
- Docs-are-done, freshness cadences, ownership: 7 and 9 converge; merged into `GOV-*`.
- Maturity/audit process: 7 (OpenDocs-derived) and 9's coverage map are complementary halves: 7 audits *structure and coverage*, 9 audits *truth of claims*. Both retained.

## 5. Conflicts and resolutions (conflict-resolution log)

| # | Conflict | Positions | Resolution |
|---|---|---|---|
| C1 | Iterate vs plan | diataxis.fr prescribes small iterative steps, discourages planning and top-down workflows; peterknego (and the deliverable-driven workflows in 7/9) run gated, planned batch runs | Not a factual conflict — a difference in scope (hand improvement vs delegated generation). Both retained as modes: the default improvement loop is iterative (`WRITE-ITER-1`); batch generation/restructuring must use the gated plan workflow (`SAFE-GATE-*`), which peterknego's own design doc justifies as "a good-faith translation of the workflow into a tool". SKILL.md router sends small edits to the iterative mode and set-level work to the gated mode. |
| C2 | Four sections always? | anivar's IA reference recommends a full top-level structure incl. getting-started/tutorials/guides/reference/concepts; diataxis.fr + sammcj + peterknego forbid manufacturing empty types | Canonical: never create a type without genuine material (diataxis.fr controls doctrine). anivar's structure retained as the *target shape when the material exists* (`ARCH-IA-1`), explicitly subordinated to the anti-scaffold rule (`DIA-WORK-3`). |
| C3 | Tutorial voice: "we" vs "you" | diataxis.fr/canonical: first-person plural; Google/Microsoft overrides: always "you" | Compatible optional modes. Default is Diátaxis per-quadrant voice; style overrides are explicit opt-in overlays (`WRITE-STYLE-OVR-1`), exactly as anivar models it. |
| C4 | Uniform vs per-quadrant tone | Google/Microsoft: uniform conversational/warm voice everywhere; Diátaxis: tone varies by type (austere reference) | Same as C3 — configurable style system; the default enforces `style-tone-matches-type`. |
| C5 | Opinion in docs | Diátaxis: opinion belongs in explanation; Google override: avoid opinion everywhere | Configurable via style override; default follows Diátaxis (opinion welcome in explanation only). |
| C6 | Auto-generated reference | anivar minimal style: "auto-generated is fine for now"; anivar API-reference anti-pattern: "auto-generated without human review"; diataxis.fr: generation is powerful but not all that's required; peterknego: autodoc rule — never write parallel pages | Layered rule, no real conflict: (a) generation is legitimate and keeps reference faithful (8); (b) where a generator exists, improve its inputs, never write parallel pages (4); (c) generated output still gets human review and the doctrine's structure/consistency demands (7); (d) under the Minimal override, generated-only reference is an accepted starting point (7). Encoded in `WRITE-REF-7` + `ARCH-AUTODOC-1`. |
| C7 | Navigation depth: two levels max vs deeper hierarchy | anivar: `arch-two-level-max`; diataxis.fr complex-hierarchies + peterknego structure sheet: add a hierarchy layer rather than fork a page; "documentation should be as complex as it needs to be" | Resolved as complementary defaults: prefer broad-shallow, two levels as the working default (7); when a page would otherwise carry forks or a list breaks the ~7-item rule, adding a properly-landing-paged layer is the sanctioned exception (8). Encoded in `ARCH-IA-3`/`ARCH-IA-4`. Disagreement preserved and stated in `references/information-architecture.md`, not flattened. |
| C8 | Where reading source code ranks as evidence | docs-and-release-rig: reading implementation ranks *below* a published dev note (code may be mid-refactor/flagged/reverted); the evidence ladder elsewhere (and verify-readme-features) treats source as strong evidence | Scope difference, resolved by target-state: for **pre-release/moving targets**, the source hierarchy applies (dev note over code-reading, runtime test over both). For **shipped, stable code**, source + runtime evidence is the top of the ladder (claims verified against source per peterknego's reference sheet, then by execution). Encoded in `EVID-HIER-1/2` with the distinction stated. |
| C9 | Troubleshooting classification | rlespinasse: lookup-table troubleshooting = Reference, step-diagnostic = How-to; keithpatton: troubleshooting usually how-to ("How to fix X" vs "Understanding why X" = explanation); anivar: troubleshooting = task-oriented content type | Merged into one richer rule: classify by structure and intended use — lookup table → reference-like; diagnostic sequence → how-to; "why X happens" → explanation; anivar's symptom-first troubleshooting *content type* retained as the recommended how-to-family form (`DIA-BOUND-1`, `WRITE-CT-6`). All three sources credited. |
| C10 | Complex-hierarchies page authority | The page was removed from diataxis.fr (commit `abfae1f`); three downstream sources still carry and rely on its guidance | Retained, labeled: guidance included with a provenance note that the page is no longer on the live site but was authored by Procida and survives in the repo history; nothing in the current site contradicts it. If future diataxis.fr guidance supersedes it, the official site wins. |
| C11 | Marketplace listing vs repository | Listing says the skill installs from `Skills_disabled/` path indirectly; repo has it disabled | Repo (first-party) wins for content; listing used only for install metadata + popularity context; the disabled state is recorded (the skill content is unaffected). |

## 6. Known gaps / accessibility notes

- All nine sources were retrieved successfully; no source is marked inaccessible.
- The diataxis.fr **complex-hierarchies** page is absent from the current site; recovered from repo git history (`git show` at the pre-removal commit) and cross-confirmed against peterknego's 2026-08-02 site mirror. Treated per conflict C10.
- Source 5 (marketplace) displays no raw SKILL.md content; the first-party repo (source 6) supplied all content. No gap results.
- agent-skills eval **runner** internals (`eval-provider.js` beyond the provider preamble, `generate-eval-config.js`) were head-read/skimmed; the eval *schema*, *validator contract*, and *runner shape* (promptfoo + `claude -p`, ~2 calls/eval) are captured. The uncaptured remainder is tool plumbing with no additional documentation-engineering capability. Recorded as a deliberate skim, not an omission.
